## Resubmission

This is a resubmission to address feedback from the CRAN maintainer team and previous checks.

### Maintainer Feedback Changes:
*   **References**: Added full references (authors, year, DOI) for g:Profiler, biomaRt, and Open Targets in the `DESCRIPTION` file.
*   **Documentation**: Added `\value` tags to all exported methods, specifically fixing `pipe.Rd`.
*   **Examples**:
    *   Replaced `\dontrun{}` with `\donttest{}` for examples requiring network access (API calls) to avoid errors on offline check servers while remaining testable.
    *   Unwrapped `plot_enrichment_dotplot` example by creating a self-contained dummy dataset, ensuring it is executable in < 5 seconds.

### Previous Fixes:
*   **LICENSE Fix**: The `LICENSE` file complies with "MIT + file LICENSE" (containing only Year/Copyright).
*   **Spelling**: "translational" is verified as a correct term (false positive).

## Test environments

*   **Local:** macOS 14.1 (aarch64), R 4.5.2
*   **R-hub:** windows-r-devel, linux-r-devel
*   **win-builder:** r-devel

## R CMD check results

*   **0 ERRORs**
*   **0 WARNINGs**
*   **0 NOTEs**